Implementation of Lenstras elliptic curve factorization algoritm.

Commands to run: 
  
  cargo run

or with optimizations:
  
  cargo run --release
  
The program takes the number to be factorized as user input from the command line.

To run the program you have to have the rustup toolchain installed.
